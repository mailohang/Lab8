name: lab7
description: A new Flutter project.

environment:
  sdk: '^3.11.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.2
  path: ^1.9.0
  file_picker: ^8.1.2
  excel: ^4.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/emails.json